import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mechanical-workshop-app',
  webDir: 'www'
};

export default config;
